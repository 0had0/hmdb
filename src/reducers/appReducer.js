import actions from '../actions/appActions'
const initState = {}

export default (action, state = initState) => {
    switch(action.type) {
        default: {
            return state
        }
    }
} 
